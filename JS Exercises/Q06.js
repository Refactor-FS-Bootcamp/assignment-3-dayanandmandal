// Q.6 Write a JS code to find the power of a number using for loop
let numberForPower = 5;
let power = 3;
let answerForPower = 1;
while( power-- > 0 ) {
    answerForPower *= numberForPower;
}
console.log(answerForPower);
